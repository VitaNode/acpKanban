### 2026-03-09 Permanent Facts
**Permanent Facts:**

*   User's primary sending email: `fanthink.com@gmail.com`.
*   Recipient email for tests: `pathtou2@gmail.com`.
*   The system utilizes the `gws` tool for Google Workspace actions.
*   Google authorization links expire.
*   `code_challenge` in Google OAuth must be base64 encoded.
*   Google OAuth requires specific, registered scopes; unregistered scopes or restricted clients lead to 403 errors.

**User Preferences:**

*   Wishes to send a test email.
*   Allows the system to decide the email title and content.
*   Prefers to use `fanthink.com@gmail.com` for sending.
*   Expects the system to fix reported errors and regenerate expired links.
*   Has installed the `gws` tool locally.
*   Instructed to create virtual environments under `workspace/task_venv` for Python libraries.

**System States:**

*   Initially lacked `gmail.send` permission.
*   Previous authorization attempts resulted in "400: invalid_request" (code_challenge encoding) and "403: restricted_client" (unregistered scopes/client ID).
*   Experienced temporary internal "capacity exhausted" and "FatalCancellationError."
*   Authorization links provided by the system have timed out.
*   Successfully sent a test email using the `gws` tool from `fanthink.com@gmail.com` to `pathtou2@gmail.com`.
*   System is ready to configure `gws` upon receiving a valid verification code.
